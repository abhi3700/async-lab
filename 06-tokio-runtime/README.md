# 06 — Tokio runtime

**Status:** Planned

Study Tokio only after the machinery it packages is understood.

## Learning goals

- Expand `#[tokio::main]` into runtime creation and `block_on`.
- Compare current-thread and multi-thread runtimes.
- Configure worker threads, drivers, and the blocking pool.
- Map Tokio runtime concepts onto the executor/reactor model.
