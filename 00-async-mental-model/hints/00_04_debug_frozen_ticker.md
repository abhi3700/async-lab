Polling a future is ordinary synchronous execution on a runtime worker. Ask which call prevents that
poll from returning control to the executor. Then distinguish **waiting** from **blocking** and
**CPU-heavy computation**: each belongs behind a different boundary.
