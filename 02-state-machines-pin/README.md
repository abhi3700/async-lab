# 02 — State machines and `Pin`

**Status:** Planned

Make the compiler-generated state behind `async` code concrete.

## Learning goals

- Sketch the states generated by multiple `.await` points.
- Identify data that must survive suspension.
- Explain self-references, `Pin`, and `Unpin`.
- Compare stack pinning, `Box::pin`, and safe projection.
